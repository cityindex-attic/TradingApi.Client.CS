NB!  The CityIndex repository dlls use a custom version of Log4Net, which is not signed.

If using an external dll that requires a signed log4net 1.2.10.0, reference the .dll in the 1.2.10.0 folder, 
or use a binding redirect in the .config file